# Cursor cloud agents build their environment from this file. Copy it and
# environment.json into your repository's .cursor/ directory; Cursor checks
# your repository out itself, so nothing here copies it.
#
# Pin the tag to the mobai-dev version you want; the setup script the
# environment runs at install refreshes mobai-dev to the current build either
# way.
FROM ghcr.io/mobai-app/mobai-dev:1.2.0
